# whatot-base

Archived CS reading notes and small experiments.

Kept here:

- `cplus/`, `python/`, `security/`, `kernel/`: old notes and low-dependency examples.
- `golang/`: small Go examples.
- `hecto/`, `rs-kameo/`, `rs-shelter/`, `rust/`: Rust projects still worth keeping runnable.

Removed noisy dependency samples and old Docker/Java/Raft/ML toy projects. If a
new experiment needs active dependency maintenance, put it in its own repo.
