p22
练习2.2 为什么echo程序不能使用getopt函数?
	解答:(1)可能是getopt()函数不可用的情况下,最小化安装的需求;
		(2)参数解析时,命令行中如果--参数存在,echo的行为会与所需的行为不同
练习2.3 STREQ宏的优缺点,C编译器如何优化strcmp调用的?
	#define STREQ(a, b) (*(a) == *(b) && strcmp((a), (b)) == 0)
	很多情况下比较的两个字符串首字母不同,因此提高速度
